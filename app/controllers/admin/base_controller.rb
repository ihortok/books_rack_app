# frozen_string_literal: true

require "#{ENV['APP_FULL_PATH']}/config/db"
require "#{ENV['APP_FULL_PATH']}/controllers/base_controller"

module Admin
  # BaseController
  class BaseController < BaseController; end
end
